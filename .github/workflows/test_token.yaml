name: Testing of the action

on:
  push:
    branch:
      - main

jobs:
  checkout-repo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: run action for repo ${{ REPO_URL }}
        uses: ./
        with:
          repo_url: ${{ REPO_URL }}
          username: ${{ secrets.AZURE_USERNAME }}
          token: ${{ secrets.AZURE_TOKEN }}
      - name: repository content
        run: |
          ls